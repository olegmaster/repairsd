# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    name:
        label: Name
    email:
        label: email
    message:
        label: message
    created_at:
        label: created at
